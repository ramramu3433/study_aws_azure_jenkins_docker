/// <reference types="q" />
/*!---------------------------------------------------------
 * Copyright (C) Microsoft Corporation. All rights reserved.
 *----------------------------------------------------------*/
import * as adal from "adal-node";
import { Promise } from "q";
import * as index from "../index";
import ProviderBase from "../provider-base";
export default class Provider extends ProviderBase<index.AuthorizationCodeGrantFlowSettings> {
    private getAuthorizationCode;
    constructor(getAuthorizationCode: index.AuthorizationCodeCallback, settings?: index.AuthorizationCodeGrantFlowSettings, tokenCache?: adal.TokenCache);
    protected authenticate(tenantId: string, msa: boolean, userId: string, silent: boolean): Promise<any>;
}
