/*!---------------------------------------------------------
 * Copyright (C) Microsoft Corporation. All rights reserved.
 *----------------------------------------------------------*/
import * as adal from "adal-node";
export default class JsonFileTokenCache implements adal.TokenCache {
    private filename;
    private activeOperation;
    constructor(dir?: string, filename?: string, noProbe?: boolean);
    private doOperation<T>(op);
    private readCache();
    protected findInCache(cache: adal.TokenCacheEntry[], query: adal.TokenCacheQuery): any[];
    find(query: adal.TokenCacheQuery, callback: (error: Error, results: adal.TokenCacheEntry[]) => void): void;
    private writeCache(cache);
    protected addToCache(cache: adal.TokenCacheEntry[], entries: adal.TokenCacheEntry[]): any[];
    add(entries: adal.TokenCacheEntry[], callback: (error?: Error) => void): void;
    protected removeFromCache(cache: adal.TokenCacheEntry[], entries: adal.TokenCacheEntry[]): any[];
    remove(entries: adal.TokenCacheEntry[], callback: (error?: Error) => void): void;
}
