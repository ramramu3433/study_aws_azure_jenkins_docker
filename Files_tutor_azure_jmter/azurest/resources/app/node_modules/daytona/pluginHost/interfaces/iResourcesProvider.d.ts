import { Promise } from "q";
import { IEventTarget } from "../common/event";
import { Resources } from "../common/resources";
export interface IResourcesProvider extends IEventTarget {
    loadResources(path: string, alias: string): Promise<Resources>;
    getDefaultErrorMessage(): string;
}
