export declare enum FileAccess {
    read = 1,
    write = 2,
    readWrite = 3,
}
export declare enum FileMode {
    createNew = 1,
    create = 2,
    open = 3,
    openOrCreate = 4,
    truncate = 5,
    append = 6,
}
export declare enum FileShare {
    none = 0,
    read = 1,
    write = 2,
    readWrite = 3,
    delete = 4,
}
export declare enum FileType {
    binary = 0,
    text = 1,
}
export declare enum FilePersistence {
    permanent = 0,
    temporary = 1,
}
export interface FileOptions {
    access?: FileAccess;
    encoding?: string;
    mode?: FileMode;
    share?: FileShare;
    persistence?: FilePersistence;
    type?: FileType;
}
