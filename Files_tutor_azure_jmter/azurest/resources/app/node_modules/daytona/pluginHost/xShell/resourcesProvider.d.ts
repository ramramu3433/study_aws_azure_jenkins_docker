import { IResourcesProvider } from "../interfaces/iResourcesProvider";
import { IFileProvider } from "../interfaces/iFileProvider";
import { Resources } from "../common/resources";
import { EventTarget } from "../common/event";
import { Promise } from "q";
export declare class ResourcesProvider extends EventTarget implements IResourcesProvider {
    private fileProvider;
    private static genericError;
    private currentCulture;
    constructor(fileProvider: IFileProvider, defaultCulture: string);
    loadResources(directoryPath: string, filePath: string): Promise<Resources>;
    updateCulture(newCulture: string): void;
    getDefaultErrorMessage(): string;
}
