import { ThemeMap } from "../common/themeMap";
import { IThemeProvider } from "../interfaces/iThemeProvider";
import { IFileProvider } from "../interfaces/iFileProvider";
import { EventTarget } from "../common/event";
import { Promise } from "q";
export declare class ThemeProvider extends EventTarget implements IThemeProvider {
    private fileProvider;
    private currentTheme;
    private themeDirectory;
    constructor(fileProvider: IFileProvider, themeDirectory: string, currentTheme?: string);
    getPluginCss(): Promise<string>;
    getThemeMap(): Promise<ThemeMap>;
    updateTheme(newTheme: string): void;
}
