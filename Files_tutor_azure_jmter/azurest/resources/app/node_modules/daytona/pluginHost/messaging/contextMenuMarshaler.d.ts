import { EventTarget } from "../common/event";
import { Point } from "../common/point";
import { PositionInfo } from "../common/positionInfo";
import { SynchronousJsonPortMarshaler } from "./marshaler";
import { IContextMenuProvider } from "../interfaces/iContextMenuProvider";
import { Promise } from "q";
export declare class ContextMenuMarshaler extends SynchronousJsonPortMarshaler {
    contextMenuProvider: IContextMenuProvider;
    constructor(control: EventTarget, contextMenuProvider: IContextMenuProvider);
    callback(id: string): Promise<any>;
    dismissAll(): Promise<any>;
    dismissCurrent(ignoreDismissForRoot: boolean): Promise<any>;
    dismissSubmenus(currentCoordinates: Point): Promise<any>;
    contentready(): Promise<any>;
    show(id: string, ariaLabel: string, contextMenuHtml: string, positionInfo: PositionInfo): Promise<any>;
    contextMenuDismissed(): void;
}
