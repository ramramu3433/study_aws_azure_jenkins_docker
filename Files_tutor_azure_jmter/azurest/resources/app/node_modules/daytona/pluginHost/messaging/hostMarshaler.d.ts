import { JsonPortMarshaler } from "./marshaler";
import { IHostProvider } from "../interfaces/iHostProvider";
import { Promise } from "q";
export declare class HostMarshaler extends JsonPortMarshaler {
    hostProvider: IHostProvider;
    constructor(hostProvider: IHostProvider);
    showDocument(path: string, line: number, column: number): Promise<void>;
    getDocumentLocation(path: string): Promise<string>;
}
