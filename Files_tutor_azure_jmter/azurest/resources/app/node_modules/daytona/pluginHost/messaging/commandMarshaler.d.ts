import { EventTarget } from "../common/event";
import { ICommandState } from "../common/iCommandState";
import { ICommandsProvider } from "../interfaces/iCommandsProvider";
import { SynchronousJsonPortMarshaler } from "./marshaler";
import { Promise } from "q";
export declare class CommandsMarshaler extends SynchronousJsonPortMarshaler {
    menuAliases: string[];
    commandAliases: string[];
    commandsProvider: ICommandsProvider;
    constructor(control: EventTarget, menuAliases: string[], commandAliases: string[], commandsProvider: ICommandsProvider);
    setCommandsStates(states: ICommandState[]): Promise<void>;
    showContextMenu(menuName: string, xPosition: number, yPosition: number): Promise<void>;
    fireCommandExecuted(commandName: string): void;
    initializeState(): void;
}
