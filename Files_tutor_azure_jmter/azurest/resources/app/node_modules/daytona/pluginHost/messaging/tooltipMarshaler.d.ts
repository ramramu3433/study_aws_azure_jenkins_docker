import { JsonPortMarshaler } from "./marshaler";
import { ITooltipProvider } from "../interfaces/iTooltipProvider";
import { PopupDisplayParameters } from "../common/popupDisplayParameters";
export declare class TooltipMarshaler extends JsonPortMarshaler {
    tooltipProvider: ITooltipProvider;
    constructor(tooltipProvider: ITooltipProvider);
    hostContentInPopup(config: PopupDisplayParameters): void;
    dismissPopup(): void;
}
