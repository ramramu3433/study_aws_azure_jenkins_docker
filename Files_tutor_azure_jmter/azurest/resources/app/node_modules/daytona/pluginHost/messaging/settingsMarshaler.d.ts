import { EventTarget } from "../common/event";
import { SynchronousJsonPortMarshaler } from "./marshaler";
import { ISettingsProvider } from "../interfaces/iSettingsProvider";
import { Promise } from "q";
export declare class SettingsMarshaler extends SynchronousJsonPortMarshaler {
    settingsProvider: ISettingsProvider;
    constructor(control: EventTarget, settingsProvider: ISettingsProvider);
    get(collection: string, keys: string[]): Promise<any>;
    set(collection: string, toSet: any): void;
}
