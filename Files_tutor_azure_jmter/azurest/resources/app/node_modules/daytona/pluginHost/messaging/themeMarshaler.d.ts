import { EventTarget } from "../common/event";
import { SynchronousJsonPortMarshaler } from "./marshaler";
import { IFileProvider } from "../interfaces/iFileProvider";
import { ThemeMap } from "../common/themeMap";
import { Promise } from "q";
export declare class ThemeMarshaler extends SynchronousJsonPortMarshaler {
    fileProvider: IFileProvider;
    themeMap: ThemeMap;
    pluginCss: string;
    constructor(control: EventTarget, fileProvider: IFileProvider, themeMap: ThemeMap, pluginCss: string);
    getCssFile(url: string): Promise<string>;
    fireThemeChangedEvent(themeMap: ThemeMap): void;
    initializeState(): void;
}
