import { EventTarget } from "../common/event";
import { SynchronousJsonPortMarshaler } from "./marshaler";
import { ResourceMap } from "../common/resourceMap";
export declare class ResourcesMarshaler extends SynchronousJsonPortMarshaler {
    resourceMap: ResourceMap;
    defaultAlias: string;
    genericError: any;
    constructor(control: EventTarget, resourceMap: ResourceMap, defaultAlias: string, genericError: any);
    private getResourceMapEventArgs();
    initializeState(): void;
    fireResourcesChangedEvent(resourceMap: ResourceMap): void;
}
