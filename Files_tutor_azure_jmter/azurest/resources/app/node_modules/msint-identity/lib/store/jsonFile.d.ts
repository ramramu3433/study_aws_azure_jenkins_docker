/// <reference types="q" />
/*!---------------------------------------------------------
 * Copyright (C) Microsoft Corporation. All rights reserved.
 *----------------------------------------------------------*/
import { Promise } from "q";
import * as index from "../index";
import AccountStore from "./base";
export default class JsonFileAccountStore extends AccountStore {
    private filename;
    constructor(dir?: string, filename?: string, noProbe?: boolean);
    protected readAccounts(): Promise<index.Account[]>;
    protected writeAccounts(accounts: index.Account[]): Promise<void>;
}
